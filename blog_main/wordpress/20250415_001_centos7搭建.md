---
slug: "wordpress/20250415_001"
title: centos7安装wordpress
tags: [centos, wordpress]
---


### 环境准备

备份原有的yum源文件，更新yum源为阿里云

```bash
curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo

yum clean cache

yum makecache

yum update
```

确认防火墙当前状态

```bash
systemctl status firewalld.service
```

关闭防火墙
```bash
systemctl stop firewalld.service
systemctl disable firewalld.service
```

关闭Selinux,修改为SELINUX=disabled

```bash
[root@localhost ~]# vi /etc/selinux/config

# This file controls the state of SELinux on the system.
# SELINUX= can take one of these three values:
#     enforcing - SELinux security policy is enforced.
#     permissive - SELinux prints warnings instead of enforcing.
#     disabled - No SELinux policy is loaded.
SELINUX=disabled
# SELINUXTYPE= can take one of three values:
#     targeted - Targeted processes are protected,
#     minimum - Modification of targeted policy. Only selected processes are protected.
#     mls - Multi Level Security protection.
SELINUXTYPE=targeted
```

### 安装nginx

nginx多wordpress配置

```bash
server {
    listen       80;
    server_name  localhost;

    #charset koi8-r;

    #access_log  logs/host.access.log  main;
    root /usr/local/nginx/html;
    index index.php index.html index.htm;
    
    # 确保传递这些关键参数
    fastcgi_param HTTP_HOST $host;
    fastcgi_param SERVER_NAME $server_name;
    fastcgi_param REQUEST_URI $request_uri;
        
    location / {
        
        try_files $uri $uri/ /index.php?$args;
    }
    
    location ~* \.(js|css|png|jpg|jpeg|gif|ico)$ {
        expires max;
        log_not_found off;
    }
    
    location /ro {
        try_files $uri $uri/ /ro/index.php?$args;
    }
    
    location /en {
        try_files $uri $uri/ /en/index.php?$args;
    }
    location /cn {
        try_files $uri $uri/ /en/index.php?$args;
    }
    
    #location /ro/ {
    #	# root html/ro;
    #	index  index.php index.html index.htm;
    #	try_files $uri $uri/ /ro/index.php?$args;
    #	
    #	# WordPress PHP处理
    #	location ~ \.php$ {
    #		#root /usr/local/nginx/html/ro;    
    #		include fastcgi_params;   
    #		fastcgi_pass 127.0.0.1:9000;   
    #		fastcgi_index index.php;
    #		fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
    #	}
    #}
    
    
    location ~ \.php$ {
        #root /usr/local/nginx/html;    
        fastcgi_pass 127.0.0.1:9000;  
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    #location ~ \.php$ {
    #    root           html;
    #    fastcgi_pass   127.0.0.1:9000;
    #    fastcgi_index  index.php;
    #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
    #    include        fastcgi_params;
    #}

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}

```

保存配置并重启 Nginx 


### 安装mysql

### 安装PHP

更新yum源
```bash
sudo yum install \
https://repo.ius.io/ius-release-el7.rpm \
https://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm
```

运行以下命令添加Webtatic源

```bash
sudo rpm -Uvh https://mirror.webtatic.com/yum/el7/webtatic-release.rpm
```
安装PHP

```bash
sudo yum install http://rpms.remirepo.net/enterprise/remi-release-7.rpm
sudo yum-config-manager --enable remi-php74
yum install php php-cli
sudo yum install php-fpm php-mysqlnd php-gd php-json php-mbstring
yum install php-zip php-devel php-mcrypt php-curl php-xml php-pear php-bcmath 



sudo systemctl start php-fpm
sudo systemctl enable php-fpm
```

**配置 PHP-FPM**
检查用户是否存在
```
id nginx
```
如果系统返回了用户的 UID 和 GID，那么用户存在。如果返回错误信息，比如“id: 'nginx': 没有那个用户”，那么你需要创建这个用户。

```
sudo useradd -r -s /bin/false nginx
```

检查组是否存在
```
getent group nginx
```

如果组不存在，你可以使用 groupadd 命令创建它：

```
sudo groupadd nginx
```

将用户添加到组

```
sudo usermod -a -G nginx nginx
```


编辑 /etc/php-fpm.d/www.conf 文件，更改 user 和 group 为 nginx：

```bash
sudo vi /etc/php-fpm.d/www.conf
```

找到并更改以下行：

```bash
user = nginx
group = nginx
```

保存并重启
```bash

sudo systemctl restart php-fpm
```


### 安装Wordpress


为WordPress创建一个MySQL数据库和用户

```bash
//登录 MySQL 的root(管理)帐户:
mysql -u root -p

//创建一个 WordPress 可以控制的新数据库。您可以随意调用它,但在此示例中我将调用它wordpress。
CREATE DATABASE wordpress;


//我将调用新帐户wordpressuser并为其分配密码password。您绝对应该使用不同的用户名和密码,因为这些示例不是很安全。
CREATE USER wordpressadmin@localhost IDENTIFIED BY 'wordpressadmin';

//此时,您已经拥有了一个专门为 WordPress 制作的数据库和用户帐户。但是,用户无权访问数据库。我们需要通过授予用户对数据库的访问权限来将这两个组件链接在一起。


GRANT ALL PRIVILEGES ON *.* TO 'wordpressadmin'@'%' IDENTIFIED BY 'wordpressadmin';

FLUSH PRIVILEGES;
```


下载最新版本的wordpress包并上传到服务器上

```bash
cd /usr/local
mkdir tmp
cd tmp
wget https://wordpress.org/latest.tar.gz
tar -xzvf latest.tar.gz

# 修改wordpress文件夹为ro

sudo mv ro/* /usr/local/nginx/html/ro
sudo chown -R nginx:nginx /usr/local/nginx/html/ro
```

```bash
cd /usr/local/nginx/html/ro
sudo cp wp-config-sample.php wp-config.php

```
修改以下配置：

```bash
define('DB_NAME', 'wordpress');
define('DB_USER', 'wordpressadmin');
define('DB_PASSWORD', 'wordpressadmin');
define('DB_HOST', 'localhost');
```

此时访问ip,即可完成wordpress的安装

```bash
define('WPLANG', 'zh_CN');
define('UPLOADS', 'wp-content/uploads');

define("FS_METHOD","direct");
define("FS_CHMOD_DIR", 0777);
define("FS_CHMOD_FILE", 0777);
define( 'WP_SITEURL', '/wordpress' );
define( 'WP_ALLOW_MULTISITE', true );
```

### 常见问题解决

无法上传文件

```bash
sudo chown -R nginx:nginx /usr/local/nginx/html/cn
sudo chown -R nginx:nginx /usr/local/nginx/html/cn/wp-content
sudo chown -R nginx:nginx /usr/local/nginx/html/cn/wp-content/uploads
sudo chown -R nginx:nginx /usr/local/nginx/html/cn/wp-content/upgrade
sudo chown -R nginx:nginx /usr/local/nginx/html/cn/wp-content/themes
sudo chown -R nginx:nginx /usr/local/nginx/html/cn/wp-content/plugins


sudo chown -R nginx:nginx /usr/local/nginx/html/wordpress_main
sudo chown -R nginx:nginx /usr/local/nginx/html/wordpress_main/wp-content
sudo chown -R nginx:nginx /usr/local/nginx/html/wordpress_main/wp-content/uploads
sudo chown -R nginx:nginx /usr/local/nginx/html/wordpress_main/wp-content/upgrade
sudo chown -R nginx:nginx /usr/local/nginx/html/wordpress_main/wp-content/themes
sudo chown -R nginx:nginx /usr/local/nginx/html/wordpress_main/wp-content/plugins

#sudo chown 777 -R /usr/local/nginx/html/wordpress
#sudo chmod 777 -R /usr/local/nginx/html/wordpress/wp-content/
#sudo chmod -R 777 /usr/local/nginx/html/wordpress/wp-content/uploads

sudo chcon -R -t httpd_sys_rw_content_t /usr/local/nginx/html/ro
sudo chcon -R -t httpd_sys_rw_content_t /usr/local/nginx/html/ro/wp-content/uploads


# 或者永久更改策略
sudo semanage fcontext -a -t httpd_sys_rw_content_t "/usr/local/nginx/html/cn/wp-content/uploads(/.*)?"
sudo restorecon -Rv /usr/local/nginx/html/cn/wp-content/uploads
```

PHP扩展缺失

```
sudo yum install php-xml

sudo yum install php-curl php-json php-zip
sudo yum install php-dom php-simplexml php-xmlreader php-xmlwriter

sudo systemctl restart httpd


```


### 安装FTP服务
用yum安装FTP服务
```
yum install -y vsftpd
```

将FTP服务设为开机自启

```
systemctl enable vsftpd
```

启动FTP服务

```
systemctl start vsftpd
```


准备FTP账号
```
useradd ftpadmin

passwd ftpadmin
```


设置allow_ftpd_full_access为on。
```
setsebool -p allow_ftpd_full_access on
```


配置FTP服务

准备一个文件目录，作为连接ftp服务器后你所进去的位置，本文以/var/ftp/test来举例。

创建目录

```
mkdir /var/ftp/test
```
将该目录的owner和group都设为准备好的ftp用户和组

```
chown -R ftpuser:ftpuser /var/ftp/test
```

接下来就是编辑FTP服务的配置文件vsftpd.conf了

```
vim /etc/vsftpd/vsftpd.conf
```

将下列已有参数配置成下表所示

```
1 anonymous_enable=NO                           
禁止匿名用户登录FTP服务器

2 local_enable=YES                              
允许本地用户登录FTP服务器 

3 chroot_local_user=YES                         
本地用户登录后就自动转到他们的用户主目录

4 chroot_list_enable=YES                        
启用chroot_list_file配置项指定的用户列表文件

5 chroot_list_file=/etc/vsftpd/chroot_list      
指定chroot_list_file文件位置

6 listen=YES                                    
以独立运行的方式监听服务 

7 #listen_ipv6=YES　　　　　　　　　　　　　　　　　　
在前面加上"#"，关闭对ipv6的监听        
```

然后再添加以下参数

```bash
1 local_root=/var/ftp/test　　　　　　　　　　　　　
指定本地用户登录后转入的目录

2 allow_writeable_chroot=YES　
　　　　　　　　　　　
3 pasv_enable=YES　　　　　　　　　　　　　　　　　　
设置是否使用PASV方式来获得数据连接

4 pasv_address=xxx.xx.xxx.xx 　　　　　　　　　　　
使用PASV模式时的IP号，请修改为您的 Linux 云服务器公网 IP

5 pasv_min_port=40000                          
使用PASV模式连接时最大的端口号

6 pasv_max_port=45000                          
使用PASV模式连接时最小的端口号

```

配置完vsftpd.conf文件后，需要创建一个chroot_list文件

```bash
vim /etc/vsftpd/chroot_list
```

```bash
重启ftp服务
systemctl restart vsftpd
```



### 卸载php
移除PHP软件包
```
sudo yum remove php php-*
```

理PHP配置文件
```
sudo rm -rf /etc/php.ini
sudo rm -rf /etc/php.d
```

清理PHP缓存和日志
```
sudo rm -rf /var/lib/php
sudo rm -rf /var/log/php-fpm
```

更新系统缓存

```
sudo yum clean all
sudo yum update
```

### nginx单wordpress配置


```bash
server {
    listen       8888;
    server_name  localhost8080;
    #charset koi8-r;

    #access_log  logs/host.access.log  main;

    
    # 确保传递这些关键参数
    #fastcgi_param HTTP_HOST $host;
    #fastcgi_param SERVER_NAME $server_name;
    #fastcgi_param REQUEST_URI $request_uri;
        
    location / {
        root /usr/local/nginx/html/wordpress;
        index index.php index.html index.htm;
        #try_files $uri $uri/ /index.php?$args;
    }
    
    location ~ \.php$ {
        root /usr/local/nginx/html/wordpress;    
        fastcgi_pass 127.0.0.1:9000;   
        fastcgi_index index.php;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        include fastcgi_params;   
    }
}
```